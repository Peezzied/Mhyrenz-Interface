<UserControl x:Class="Mhyrenz_Interface.Controls.ColumnList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mhyrenz_Interface.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:ColumnList, IsDesignTimeCreatable=True}" Height="Auto" Width="461">
    <Border BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="10" Height="Auto" Padding="0,12">
        <dragablz:DragablzItemsControl AllowDrop="False" FixedItemCount="2" x:Name="DragablzItemsControl" ItemsSource="{Binding ColumnsView}" d:ItemsSource="{Binding Columns}"  Grid.Row="0">
            <dragablz:DragablzItemsControl.ItemsOrganiser>
                <dragablz:VerticalOrganiser />
            </dragablz:DragablzItemsControl.ItemsOrganiser>
            <dragablz:DragablzItemsControl.ItemContainerStyle>
                <Style TargetType="{x:Type dragablz:DragablzItem}">
                    <Style.Setters>
                        <Setter Property="IsTabStop" Value="False"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="Padding" Value="3"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Canvas.Left" Value="{Binding X, RelativeSource={RelativeSource Self}}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y, RelativeSource={RelativeSource Self}}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type dragablz:DragablzItem}">
                                    <Border Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}" BorderThickness="{TemplateBinding Border.BorderThickness}" Padding="{TemplateBinding Control.Padding}" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="{TemplateBinding Panel.Background}" SnapsToDevicePixels="True">
                                        <Grid Margin="10,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Thumb IsEnabled="{Binding IsDraggable}" Width="30" Height="30" x:Name="PART_Thumb" VerticalAlignment="Center">
                                                <Thumb.Template>
                                                    <ControlTemplate>
                                                        <Border Grid.Column="0" x:Name="Root"
                                                                 Width="24" VerticalAlignment="Stretch"
                                                                 Background="#E0E0E0"
                                                                 Cursor="SizeAll">
                                                            <iconPacks:PackIconMaterial Kind="Drag" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>

                                                        <ControlTemplate.Triggers>
                                                            <!-- Muted when disabled -->
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter TargetName="Root" Property="IsHitTestVisible" Value="False"/>
                                                                <Setter TargetName="Root" Property="Opacity" Value="0.4"/>
                                                                <Setter TargetName="Root" Property="Cursor" Value="Arrow"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                            <ContentPresenter Grid.Column="1" Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                    <Style.Triggers>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter Property="Opacity" Value=".95" />
                        </Trigger>
                        <Trigger Property="IsSiblingDragging" Value="True">
                            <Setter Property="Opacity" Value=".5" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </dragablz:DragablzItemsControl.ItemContainerStyle>
            <dragablz:DragablzItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ToggleButton Grid.Column="0" Visibility="{Binding Hidden, Converter={StaticResource BooleanToVisibilityConverter}}" IsChecked="{Binding IsVisible, Mode=TwoWay}" VerticalAlignment="Center" Style="{StaticResource ToggleButtonSwitch.Small}"/>
                        <Grid Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="14"/>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </dragablz:DragablzItemsControl.ItemTemplate>
        </dragablz:DragablzItemsControl>
    </Border>
</UserControl>
