<UserControl x:Class="Mhyrenz_Interface.Controls.StatusBar"
      xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mhyrenz_Interface.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>
    <StatusBar VerticalAlignment="Bottom" Style="{StaticResource MahApps.Styles.StatusBar}">
        <StatusBar.Background>
            <SolidColorBrush Color="{DynamicResource MahApps.Colors.Accent}"/>
        </StatusBar.Background>
        <StatusBarItem>
            <hc:UniformSpacingPanel Spacing="7" Orientation="Horizontal">
                <local:IconButtonWithTip Padding="0" TipTextColor="{StaticResource MahApps.Brushes.Gray1}" Tiptext="Barcode Settings" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                    <local:IconButtonWithTip.IconContent>
                        <iconPacks:PackIconMaterial Foreground="White" d:Kind="Barcode" Height="Auto">
                            <iconPacks:PackIconMaterial.Style>
                                <Style TargetType="iconPacks:PackIconMaterial">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsReady}" Value="True">
                                            <Setter Property="Kind" Value="Barcode"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsReady}" Value="False">
                                            <Setter Property="Kind" Value="BarcodeOff"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </iconPacks:PackIconMaterial.Style>
                        </iconPacks:PackIconMaterial>
                    </local:IconButtonWithTip.IconContent>
                </local:IconButtonWithTip>
                <TextBlock d:Text="Barcode Online">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsReady}" Value="True">
                                    <Setter Property="Text" Value="Barcode Online"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsReady}" Value="False">
                                    <Setter Property="Text" Value="Barcode Offline"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </hc:UniformSpacingPanel>
        </StatusBarItem>
    </StatusBar>
</UserControl>
