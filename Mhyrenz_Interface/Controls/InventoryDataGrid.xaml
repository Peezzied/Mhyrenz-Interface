<UserControl x:Class="Mhyrenz_Interface.Controls.InventoryDataGrid"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mhyrenz_Interface.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Inventory}" Style="{StaticResource MahApps.Styles.DataGrid.Azure}" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="Auto"/>
                <mah:DataGridNumericUpDownColumn Header="Price" Binding="{Binding Price}" Width="Auto" 
                    Minimum="0" Maximum="100" NumericInputMode="Numbers" InterceptManualEnter="True"/>
                <DataGridTextColumn Header="Net Qty" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Retail" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="List" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Total Cost" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Total List" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Profit Revenue" Binding="{Binding Price}" Width="Auto"/>
                <DataGridTextColumn Header="Profit" Binding="{Binding Price}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
